spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/imageSearch
    username: postgres
    password: postgres
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true
